{
  "name": "bitbake",
  "displayName": "bitbake",
  "description": "BitBake language support",
  "license": "MIT",
  "version": "0.0.8",
  "publisher": "EugenWiens",
  "author": {
    "name": "Eugen Wiens"
  },
  "icon": "images/icon.png",
  "bugs": {
    "url": "https://github.com/EugenWiens/vscode-bitbake/issues"
  },
  "homepage": "https://github.com/EugenWiens/vscode-bitbake",
  "repository": {
    "type": "git",
    "url": "https://github.com/EugenWiens/vscode-bitbake.git"
  },
  "engines": {
    "vscode": "^1.10.0"
  },
  "categories": [
    "Languages"
  ],
  "activationEvents": [
    "onLanguage:bitbake"
  ],
  "main": "./out/extension",
  "contributes": {
    "languages": [
      {
        "id": "bitbake",
        "aliases": [
          "BitBake",
          "bitbake"
        ],
        "extensions": [
          ".bb",
          ".bbappend",
          ".inc",
          ".bbclass",
          ".conf"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "bitbake",
        "scopeName": "source.bb",
        "path": "./syntaxes/bitbake.tmLanguage.json"
      }
    ],
    "configuration": {
      "type": "object",
      "title": "Language Server for Bitbake configuration",
      "properties": {
        "languageServerBitbake.loggingLevel": {
          "type": "string",
          "enum": [
            "off",
            "messages",
            "verbose"
          ],
          "default": "off",
          "description": "Adjust the logging level. off: no output; messages: only information that is useful for the user; verbose: the same like messages, but with additional debug informations"
        },
        "languageServerBitbake.deepExamine": {
          "type": "boolean",
          "default": false,
          "description": "Activates an deeply examine. If a recipe export an extra package that have not the same name like the recipe file the normal scan will not find this packages. If you like to find the packages you have to activate this option."
        }
      }
    },
    "commands": [
      {
        "command": "bitbake.rescan-project",
        "title": "BitBake: Rescan Project",
        "description": "This command rescans the bitbake project."
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "tsc -p ./",
    "compile": "tsc -watch -p ./",
    "update-vscode": "node ./node_modules/vscode/bin/install",
    "postinstall": "node ./node_modules/vscode/bin/install"
  },
  "devDependencies": {
    "@types/mocha": "^2.2.33",
    "@types/node": "^6.0.79",
    "typescript": "^2.4.1",
    "vscode": "^1.1.2"
  },
  "dependencies": {
    "vscode-languageclient": "^3.3.0"
  }
}
